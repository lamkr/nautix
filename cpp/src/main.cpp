/*#include <gtkmm.h>
int main(int, char**) {
    auto app = Gtk::Application::create("org.nautix.app");
    Gtk::Window window;
    window.set_title("Nautix");
    window.set_default_size(800, 600);
    return app->run(window);
}
*/